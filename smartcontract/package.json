{
  "name": "smartcontract",
  "version": "1.0.0",
  "description": "",
  "main": "truffle-config.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx kill-port 7545 && ganache -p 7545 --detach && truffle migrate --reset && node abigenerate.js",
    "build": "truffle migrate --network matic && node abigenerate.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@truffle/hdwallet-provider": "^2.1.12",
    "dotenv": "^16.1.4"
  }
}
